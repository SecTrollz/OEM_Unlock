// Root build file — only plugin version declarations live here, applied
// per-module (currently just :app). Nothing to configure at this level.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "2.0.0" apply false
}
