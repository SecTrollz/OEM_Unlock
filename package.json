{
  "name": "oem-unlock-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Proxy scripts that bypass carrier OEM-unlock restrictions on Google provisioning APIs.",
  "scripts": {
    "setup": "node scripts/setup.js",
    "start": "node scripts/start.js",
    "start-offline": "node scripts/start.js --offline",
    "build": "node build.js",
    "test": "node --test"
  },
  "license": "UNLICENSED"
}
